import { Component, OnInit } from '@angular/core';
import { HttpClient } from '@angular/common/http';

@Component({
  selector: 'app-clause-prediction',
  templateUrl: './clause-prediction.component.html',
  styleUrls: ['./clause-prediction.component.css']
})
export class ClausePredictionComponent implements OnInit {
  predictions: any[] = [];

  constructor(private http: HttpClient) { }

  ngOnInit(): void {
    // Fetch predictions from backend
    this.http.get<any[]>('http://localhost:5000/predict-clauses').subscribe(
      (data) => {
        this.predictions = data;
      },
      (error) => {
        console.log('Error fetching predictions:', error);
      }
    );
  }

  // Function to display predictions for a paragraph
  displayPredictions(index: number) {
    const prediction = this.predictions[index];
    alert(`Section: ${prediction.section}, Clause: ${prediction.clause}, Sub-clause: ${prediction.subSection}`);
  }
}
-----------
<div style="text-align:center">
  <h1>
    Document Analysis App
  </h1>
</div>

<div>
  <!-- Upload Document Component -->
  <app-pdf-upload></app-pdf-upload>
</div>

<div>
  <!-- Classify Document Component -->
  <app-document-type></app-document-type>
</div>

<div>
  <!-- Clause Prediction Component -->
  <app-clause-prediction></app-clause-prediction>
</div>
