app.component.html
html
Copy code
<div class="container">
  <h1>Document Classification and Clause Prediction</h1>
  <app-upload-pdf></app-upload-pdf>
  <app-predict-clause></app-predict-clause>
</div>
upload-pdf.component.html
html
Copy code
<div class="upload-container">
  <h2>Upload PDF</h2>
  <input type="file" (change)="onFileSelected($event)" />
  <button (click)="onUpload()">Upload</button>
  <div *ngIf="documentType">
    <h3>Document Type: {{ documentType }}</h3>
  </div>
  <div *ngIf="extractedText">
    <h3>Extracted Text:</h3>
    <p>{{ extractedText }}</p>
  </div>
</div>
upload-pdf.component.ts
typescript
Copy code
import { Component } from '@angular/core';
import { HttpClient, HttpErrorResponse } from '@angular/common/http';

@Component({
  selector: 'app-upload-pdf',
  templateUrl: './upload-pdf.component.html',
  styleUrls: ['./upload-pdf.component.css']
})
export class UploadPdfComponent {
  selectedFile: File | null = null;
  documentType: string | null = null;
  extractedText: string | null = null;

  constructor(private http: HttpClient) {}

  onFileSelected(event: Event) {
    const input = event.target as HTMLInputElement;
    if (input.files && input.files.length > 0) {
      this.selectedFile = input.files[0];
    }
  }

  onUpload() {
    if (this.selectedFile) {
      const formData = new FormData();
      formData.append('file', this.selectedFile);

      this.http.post<any>('http://localhost:5000/upload-pdf', formData).subscribe(
        response => {
          this.documentType = response.document_type;
          this.extractedText = response.extracted_text;
        },
        (error: HttpErrorResponse) => {
          console.error('Error uploading file:', error.message);
        }
      );
    }
  }
}
predict-clause.component.html
html
Copy code
<div class="predict-container">
  <h2>Predict Clause</h2>
  <textarea [(ngModel)]="inputText" placeholder="Enter text here..."></textarea>
  <button (click)="onPredict()">Predict</button>
  <div *ngIf="clausePrediction">
    <h3>Clause Prediction:</h3>
    <pre>{{ clausePrediction | json }}</pre>
  </div>
</div>
predict-clause.component.ts
typescript
Copy code
import { Component } from '@angular/core';
import { HttpClient, HttpErrorResponse } from '@angular/common/http';

@Component({
  selector: 'app-predict-clause',
  templateUrl: './predict-clause.component.html',
  styleUrls: ['./predict-clause.component.css']
})
export class PredictClauseComponent {
  inputText: string = '';
  clausePrediction: any;

  constructor(private http: HttpClient) {}

  onPredict() {
    const requestBody = { text: this.inputText };

    this.http.post<any>('http://localhost:5000/predict-clause', requestBody).subscribe(
      response => {
        this.clausePrediction = response.clauses;
      },
      (error: HttpErrorResponse) => {
        console.error('Error predicting clause:', error.message);
      }
    );
  }
}
app.module.ts
typescript
Copy code
import { BrowserModule } from '@angular/platform-browser';
import { NgModule } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { HttpClientModule } from '@angular/common/http';

import { AppComponent } from './app.component';
import { UploadPdfComponent } from './upload-pdf/upload-pdf.component';
import { PredictClauseComponent } from './predict-clause/predict-clause.component';

@NgModule({
  declarations: [
    AppComponent,
    UploadPdfComponent,
    PredictClauseComponent
  ],
  imports: [
    BrowserModule,
    FormsModule,
    HttpClientModule
  ],
  providers: [],
  bootstrap: [AppComponent]
})
export class AppModule { }
upload-pdf.component.css and predict-clause.component.css
css
Copy code
/* Add your custom styles here */

.upload-container, .predict-container {
  margin: 20px 0;
}

textarea {
  width: 100%;
  height: 100px;
  margin: 10px 0;
}
